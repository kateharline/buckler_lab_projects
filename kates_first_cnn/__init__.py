"""
the general structure of a CNN

/load, process the data (encoding into one-hot vector step)
    |

/design the structure of the model
    |
    --->/(separate the data into test, train and validation sets)) train the model with the data
        |
         ---> test the model with test set
            |
             ---> analyze results from testing

"""

